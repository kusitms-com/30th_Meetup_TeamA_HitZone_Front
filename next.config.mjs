/** @type {import('next').NextConfig} */
const nextConfig = {
    /*
    async rewrites() {
      return [
        // 온보딩 페이지
        {
          source: '/',  // 웹에서 사용자가 입력할 경로
          destination: '/Onboarding',  // 코드 상 디렉토리 경로
        },
        // 메인 페이지
        {
          source: '/main',  // 웹에서 사용자가 입력할 경로
          destination: '/Main',  // 코드 상 디렉토리 경로
        },
      ];
    },
    */
  };
  
  export default nextConfig;
  